SQLITE_DB_PATH=articles.db
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Cross-domain login system — must match the same value in the other three services'
# env vars exactly (OutsideFramework, globe-invest, structural_holes). Never commit the
# real value anywhere; set it directly in Railway's dashboard.
AUTH_SIGNING_SECRET=
AUTHORIZED_EMAIL=
CENTRAL_AUTH_ORIGIN=https://ofw.up.railway.app
