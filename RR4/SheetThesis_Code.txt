' ============================================================================
'  Worksheet code-behind for the "Thesis Library" view sheet (RR4 workbook),
'  Thesis Library v2 (2026-09-13).  modThesis.EnsureViewSheet writes this into
'  the sheet module itself; this file is the record / manual fallback.
'  The v1 code (ThesisDoubleClick / ThesisChange on tblThesis) is gone - that
'  table now lives on the hidden ThesisArchive sheet with no event code.
' ============================================================================
Option Explicit

' Thesis Library view (modThesis): QUERY / KEYWORD inputs, double-click header / target
Private Sub Worksheet_Change(ByVal Target As Range)
    Call ThesisViewChange(Me, Target)
End Sub

Private Sub Worksheet_BeforeDoubleClick(ByVal Target As Range, Cancel As Boolean)
    Call ThesisViewDoubleClick(Me, Target, Cancel)
End Sub
